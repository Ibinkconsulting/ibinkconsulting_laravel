const useAxiosPublic = () => {
    return {
        get: async () => ({ data: {} }),
        post: async () => ({ data: {} }),
        put: async () => ({ data: {} }),
        patch: async () => ({ data: {} }),
        delete: async () => ({ data: {} }),
    };
}

export default useAxiosPublic;
